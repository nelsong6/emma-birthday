# Static-only pod — no Azure SDK calls — so workload identity isn't strictly
# needed. Keeping the ServiceAccount for pattern consistency with landing-page.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: infra-shared
  namespace: emma-birthday
