apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: emma-birthday-tls
  namespace: emma-birthday
spec:
  secretName: emma-birthday-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - birthday.emma.romaine.life
